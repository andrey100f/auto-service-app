import { SearchFilterCustomerCardPipe } from './search-filter-customer-card.pipe';

describe('SearchFilterCustomerCardPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterCustomerCardPipe();
    expect(pipe).toBeTruthy();
  });
});
